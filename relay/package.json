{
  "name": "talk-with-buffett-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone ASR relay. Proxies Volcengine v2 streaming ASR WebSocket to HTTP+SSE so Vercel serverless can consume realtime ASR.",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
